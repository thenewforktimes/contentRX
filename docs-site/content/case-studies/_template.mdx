export const meta = {
  slug: "<kebab-case-slug>",
  project_name: "<Human-readable project name>",
  project_url: "https://<project homepage>",
  project_repo_url: "https://github.com/<org>/<repo>",
  project_license: "<SPDX id, e.g. MIT>",
  published_at: "YYYY-MM-DD",
  maintainer_approval: true,
  approved_by: "<GitHub handle / email / issue #NNN>",
  approved_at: "YYYY-MM-DD",
  teaser:
    "One sentence (≤140 chars) for the index card. Lead with the judgment call, not the project.",
  judgment_calls: [
    {
      summary: "<Short summary of the first judgment call>",
      standard_id: "<e.g. VT-05>",
      moment: "<e.g. error_recovery>",
      critique:
        "1–3 sentences on why this is a judgment call a generic linter would miss.",
      source_url: "https://github.com/<org>/<repo>/blob/<ref>/<file>#L<line>",
    },
    {
      summary: "<Second judgment call>",
      standard_id: "<>",
      moment: "<>",
      critique: "",
    },
    {
      summary: "<Third judgment call — minimum three>",
      standard_id: "<>",
      moment: "<>",
      critique: "",
    },
  ],
  linked_prs: [
    // Optional. Leave the array empty if no PRs landed yet.
    // { url: "https://github.com/<org>/<repo>/pull/123", title: "..." },
  ],
};

export const metadata = {
  title: `${meta.project_name} — content review · ContentRX case studies`,
  description: meta.teaser,
};

# ContentRX on {meta.project_name}

*One-paragraph context.* What does the project do? Who uses it? Why
did ContentRX run here — what was the maintainer's goal? Keep this
grounded in the maintainer's framing, not ContentRX's.

## The run

*One paragraph on logistics.* Which surfaces did you extract strings
from (component library, error boundaries, onboarding flow)? How
many strings? Was the content_type and moment distribution typical
for the project's category, or skewed?

Total strings evaluated: **N**
Verdict breakdown: **N pass · N review · N violation**
Run date: **YYYY-MM-DD**

## The judgment calls

Three minimum. Each one follows the pattern: what ContentRX flagged,
why a generic linter wouldn't have caught it, what the maintainer
said when you walked them through it.

### 1. {meta.judgment_calls[0].summary}

**Standard:** <code>{meta.judgment_calls[0].standard_id}</code> ·
**Moment:** <code>{meta.judgment_calls[0].moment}</code>

*The string:*

> &ldquo;<exact text>&rdquo;

*What ContentRX flagged.* One paragraph describing the flag with
the moment context — "in an error_recovery moment, VT-05 emphasizes
owning the failure; this string blames the user instead."

*Why a generic linter would miss it.* One paragraph contrasting
against the Grammarly / LanguageTool / Alex layer. Grammar is fine,
inclusive language is fine, the content-design failure is the
moment-aware call.

*What the maintainer said.* Quote or paraphrase with attribution.

### 2. {meta.judgment_calls[1].summary}

(Same structure.)

### 3. {meta.judgment_calls[2].summary}

(Same structure.)

## What the project changed

If any PRs landed, link them in the frontmatter's `linked_prs` and
summarise them here. If the maintainers chose not to change a
specific finding, say so — that's also valuable signal. The
refinement-log decision criterion applies: a finding that the
maintainer explicitly rejected stays in the post, with the
maintainer's reasoning, so readers can judge ContentRX's call
against the maintainer's call.

## About this case study

*Attribution.* The run used {meta.project_name}&apos;s public strings
under the <code>{meta.project_license}</code> license. All direct
quotes of project copy in this post are attributed inline. The
maintainers reviewed this post before publication; approval was
given by <code>{meta.approved_by}</code> on{" "}
<time dateTime={meta.approved_at}>{meta.approved_at}</time>.

*Opt out.* If you maintain {meta.project_name} and want this post
taken down, email <code>hello@contentrx.io</code> with subject
<code>[OPTOUT] {meta.project_name}</code>. The page goes down in the
next release cycle per the{" "}
[/ethics opt-out commitment](/ethics).
