# Content standards checker — web app

A browser-based React interface for checking content against standards.

## Status

The web app is functional but not yet included in this repo. It features auto-detected content types with a manual override dropdown and renders the same pass/fail results as the CLI and Figma plugin.

## Coming soon

- [ ] Source code and setup instructions
- [ ] Deployable to Vercel/Netlify with zero config
- [ ] Reads from the shared `standards/standards_library.json`
