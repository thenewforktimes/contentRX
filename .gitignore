# --- Private eval data (do NOT commit) ---
# Domain-specific eval cases are proprietary data that informs
# future content packs. Keep these local only.
evals/industry/
tools/extract_content.py

# --- API keys and secrets ---
# env files (commit the .env.local.example template; keep secrets local)
.env
.env*
!.env.local.example
*.key
*.pem

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/

# --- Node / Next.js ---
node_modules/
/.next/
/out/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/build
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- TypeScript ---
*.tsbuildinfo
next-env.d.ts

# --- Vercel ---
.vercel

# --- OS files ---
.DS_Store
Thumbs.db

# --- Editor files ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Claude Code workspace metadata ---
.claude/

# --- Eval run artifacts (regenerable) ---
evals/results/
.env*.local

# --- Local caches (regenerable) ---
.cache/

# --- Private notes / plans (do NOT commit) ---
# Internal sequencing docs — build plans, human-eval plan, dated
# strategy snapshots — live here. Out of git so they don't ship to
# the public repo while still letting Robo refer to them locally.
# Persist a copy somewhere outside the repo (e.g., ~/Desktop) for
# durability across worktrees.
_private/

# Private substrate (ADR 2026-04-25): the engine loads these at runtime
# but they're hosted in a private submodule, not the public repo.
src/content_checker/standards/private/

